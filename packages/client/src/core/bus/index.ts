/** Public surface of the bus — only what assembly (main/worker) needs. Internal files import each other directly */
export { Bus } from './bus.js';
export { PortTransport } from './port-transport.js';
